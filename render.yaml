services:
  - type: web
    name: smart-waste-ai-service
    env: python
    buildCommand: "cd ai-service && pip install -r requirements.txt"
    startCommand: "cd ai-service && gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
